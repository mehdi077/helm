# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Ignore everything by default
*

# But unignore essential files (excluding data.db)
!package.json
!package-lock.json
!README.md
.gitignore
.env.local
!next.config.ts
!tsconfig.json
!postcss.config.mjs
!eslint.config.mjs

# Also explicitly ignore data.db
data.db

# Keep directories but track only the files we want
!app/
!components/
!lib/
!docs/
!public/

# But ignore everything inside these directories except what we explicitly want
# We'll let git handle the files we want to track in these directories
